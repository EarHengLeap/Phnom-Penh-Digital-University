import React from "react";
import { Button } from "./ui/button";

const Logout = () => {
  return (
    <div>
        <Button className="bg-white-900 text-gray-700 hover:bg-gray-100 border border-gray-200 outline">
          Logout
        </Button>
    </div>
  );
};

export default Logout;
